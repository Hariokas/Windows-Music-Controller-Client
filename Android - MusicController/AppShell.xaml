<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Android___MusicController.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Android___MusicController"
    BackgroundColor="#404267"
    >

    <Shell.TitleView>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
        </Grid>
    </Shell.TitleView>
    
    <TabBar></TabBar>

    <FlyoutItem Title="Home" Route="MainPage">
        <ShellContent ContentTemplate="{DataTemplate local:MainPage}"/>
    </FlyoutItem>
    <FlyoutItem Title="Sessions" Route="SessionsPage">
        <ShellContent ContentTemplate="{DataTemplate local:SessionsPage}"/>
    </FlyoutItem>
    <FlyoutItem Title="Equalizer" Route="EqualizerPage">
        <ShellContent ContentTemplate="{DataTemplate local:EqualizerPage}"/>
    </FlyoutItem>

</Shell>
